name: gzp_Empleados

on:
  schedule:
    # 12:00 PM Nicaragua (18:00 UTC) y 5:30 PM Nicaragua (23:30 UTC)
    - cron: "0 18 * * *"
    - cron: "30 23 * * *"
  workflow_dispatch:


permissions:
  contents: write   # Permite hacer commit y push

jobs:
  fetch-odoo-empleados:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Llamar a Odoo JSON-RPC y guardar resultado
        env:
          ODOO_PASSWORD: ${{ secrets.ODOO_PASSWORD }}
        run: |
          echo "Llamando a Odoo hr.employee..."
          curl -s -X POST https://grupozp.odoo.com/jsonrpc \
          -H "Content-Type: application/json" \
          -d '{
            "jsonrpc": "2.0",
            "method": "call",
            "id": 1,
            "params": {
              "service": "object",
              "method": "execute",
              "args": [
                "grupozp-main-13656030",
                7,
                "'"$ODOO_PASSWORD"'",
                "hr.employee",
                "search_read",
                [],
                [
                  "department_id",
                  "job_id",
                  "x_studio_tipo_contrato",
                  "x_studio_cuenta_analitica",
                  "name",
                  "id",
                  "first_contract_date",
                  "x_studio_tipo_seguro_inss",
                  "identification_id",
                  "ssnid",
                  "gender",
                  "x_studio_estado_del_contrato",
                  "x_studio_transporte",
                  "x_studio_comunicacion"
                ]
              ]
            }
          }' > gzp_Empleados.json

          echo "Verificando contenido..."
          if [ ! -s gzp_Empleados.json ]; then
            echo "❌ Error: gzp_Empleados.json está vacío o no se generó correctamente."
            exit 1
          fi

          echo "✅ Archivo generado correctamente:"
          head -n 10 gzp_Empleados.json

      - name: Commit y push del JSON
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add gzp_Empleados.json
          git commit -m "Actualización automática de hr.employee desde Odoo" || echo "No hay cambios"

          intento=1
          maximo_intentos=5
          while [ $intento -le $maximo_intentos ]; do
            echo "Intento de push #$intento..."
            if git pull --rebase origin main && git push origin main; then
              echo "Push exitoso."
              break
            fi
            if [ $intento -eq $maximo_intentos ]; then
              echo "Se agotaron los intentos de push."
              exit 1
            fi
            espera=$(( (RANDOM % 5) + 3 ))
            echo "Push rechazado (probablemente otro workflow empujó al mismo tiempo). Esperando ${espera}s..."
            sleep $espera
            intento=$((intento + 1))
          done
