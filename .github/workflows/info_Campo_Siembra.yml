name: info_Campo_Siembra

on:
  schedule:
    # Cada 10 minutos entre 07:00 AM y 01:00 PM Nicaragua (13:00-19:00 UTC)
    - cron: "*/10 13-18 * 6-8 *"
    - cron: "0 19 * 6-8 *"       # 01:00 PM Nicaragua exacto
    # Cada hora entre 01:00 PM y 07:00 PM Nicaragua (19:00-01:00 UTC)
    - cron: "0 20-23 * 6-8 *"   # 02 PM - 05 PM Nicaragua
    - cron: "0 0,1 * 6-8 *"     # 06 PM y 07 PM Nicaragua
  workflow_dispatch:


permissions:
  contents: write   # Permite hacer commit y push

jobs:
  fetch-odoo-recuentos-siembra:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Llamar a Odoo JSON-RPC y guardar resultado
        env:
          ODOO_PASSWORD: ${{ secrets.ODOO_PASSWORD }}
        run: |
          echo "Llamando a Odoo Recuentos de Siembra..."
          curl -s -X POST https://infoagro.odoo.com/jsonrpc/ \
          -H "Content-Type: application/json" \
          -d '{
            "jsonrpc": "2.0",
            "method": "call",
            "id": 1,
            "params": {
              "service": "object",
              "method": "execute",
              "args": [
                "infoagro",
                2,
                "'"$ODOO_PASSWORD"'",
                "x_siembra",
                "search_read",
                [],
                [
                  "id","x_name","x_studio_tipo","x_studio_fecha","x_studio_ciclo",
                  "x_studio_finca","x_studio_lote_de_finca","x_studio_variedad",
                  "x_studio_calibre","x_studio_lote_comasa","x_studio_qq_sembrados",
                  "x_studio_mzn_sembrada","x_studio_semilla_entre_x_mtrs_lineal",
                  "x_studio_sector_1","x_studio_semilla_quebradas_x_mtrs_lineal",
                  "x_studio_semilla_total","x_studio_porcenjate_enteras",
                  "x_studio_porcentaje_quebradas","x_studio_remision_comasa",
                  "x_studio_company_id"
                ]
              ]
            }
          }' > info_Campo_Siembra.json

          echo "Verificando contenido..."
          if [ ! -s info_Campo_Siembra.json ]; then
            echo "❌ Error: info_Campo_Siembra.json está vacío o no se generó correctamente."
            exit 1
          fi

          echo "✅ Archivo generado correctamente:"
          head -n 10 info_Campo_Siembra.json

      - name: Commit y push del JSON
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add info_Campo_Siembra.json
          git commit -m "Actualización automática de datos Recuentos de Siembra desde Odoo" || echo "No hay cambios"
          intento=1
          maximo_intentos=5
          while [ $intento -le $maximo_intentos ]; do
            echo "Intento de push #$intento..."
            if git pull --rebase origin main && git push origin main; then
              echo "Push exitoso."
              break
            fi
            if [ $intento -eq $maximo_intentos ]; then
              echo "Se agotaron los intentos de push."
              exit 1
            fi
            espera=$(( (RANDOM % 5) + 3 ))
            echo "Push rechazado (probablemente otro workflow empujo al mismo tiempo). Esperando ${espera}s..."
            sleep $espera
            intento=$((intento + 1))
          done
