name: Odoo_DiasHombreMOGZP

on:
  schedule:
    - cron: "0 */2 * * *"   # Ejecuta cada 2 horas
  workflow_dispatch:

jobs:
  extract-odoo-data:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install jq
        run: sudo apt-get install jq -y

      - name: Query Odoo hr.payslip.worked_days
        run: |
          RESPONSE=$(curl -s -X POST "https://grupozp-main-13656030.odoo.com/jsonrpc" \
            -H "Content-Type: application/json" \
            -d '{
              "jsonrpc": "2.0",
              "method": "call",
              "id": 1,
              "params": {
                "service": "object",
                "method": "execute",
                "args": [
                  "grupozp-main-13656030",
                  "7",
                  "Meganet37**",
                  "hr.payslip.worked_days",
                  "search_read",
                  [],
                  [
                    "x_studio_nombre_del_lote",
                    "x_studio_fecha_final_lote",
                    "x_studio_cuenta_analitica_en_recibo_de_pago",
                    "x_studio_puesto_historico",
                    "x_studio_empleado",
                    "x_studio_actividad_agricola_1",
                    "x_studio_dias_efectivos",
                    "x_studio_septimo_dia",
                    "x_studio_horas_extras",
                    "x_studio_alimentacion",
                    "x_studio_feriado"
                  ]
                ]
              }
            }')

          echo "$RESPONSE" | jq '.result // .error' > Odoo_DiasHombreMOGZP.json

      - name: Commit JSON output
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add Odoo_DiasHombreMOGZP.json
          git commit -m "Updated Odoo_DiasHombreMOGZP.json" || echo "No changes to commit"
          git push
