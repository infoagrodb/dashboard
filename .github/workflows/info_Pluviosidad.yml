name: Odoo Proxy Pluviosidad

on:
  schedule:
    # Cada 10 minutos entre 07:00 AM y 01:00 PM Nicaragua (13:00-19:00 UTC)
    - cron: "*/10 13-18 * * *"
    - cron: "0 19 * * *"       # 01:00 PM Nicaragua exacto
    # Cada hora entre 01:00 PM y 07:00 PM Nicaragua (19:00-01:00 UTC)
    - cron: "0 20-23 * * *"   # 02 PM - 05 PM Nicaragua
    - cron: "0 0,1 * * *"     # 06 PM y 07 PM Nicaragua
  workflow_dispatch:


permissions:
  contents: write   # Permite hacer commit y push

jobs:
  fetch-odoo-pluviosidad:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Llamar a Odoo JSON-RPC y guardar resultado
        env:
          ODOO_PASSWORD: ${{ secrets.ODOO_PASSWORD }}
        run: |
          echo "Llamando a Odoo Pluviosidad..."
          curl -s -X POST https://infoagro.odoo.com/jsonrpc/ \
          -H "Content-Type: application/json" \
          -d '{
            "jsonrpc": "2.0",
            "method": "call",
            "id": 1,
            "params": {
              "service": "object",
              "method": "execute",
              "args": [
                "infoagro",
                2,
                "'"$ODOO_PASSWORD"'",
                "x_pluviosidad",
                "search_read",
                [],
                [
                  "id","x_studio_finca","x_studio_fecha","x_studio_cantidad_promedio",
                  "x_studio_cantidad_de_pluviometros","x_studio_pluviometro_1",
                  "x_studio_pluviometro_2","x_studio_pluviometro_3",
                  "x_studio_pluviometro_4","x_studio_pluviometro_5",
                  "x_studio_sector_1","x_studio_company_id"
                ]
              ]
            }
          }' > info_Pluviosidad.json

          echo "Verificando contenido..."
          if [ ! -s info_Pluviosidad.json ]; then
            echo "❌ Error: info_Pluviosidad.json está vacío o no se generó correctamente."
            exit 1
          fi

          echo "✅ Archivo generado correctamente:"
          head -n 10 info_Pluviosidad.json

      - name: Commit y push del JSON
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add info_Pluviosidad.json
          git commit -m "Actualización automática de datos Pluviosidad desde Odoo" || echo "No hay cambios"
          intento=1
          maximo_intentos=5
          while [ $intento -le $maximo_intentos ]; do
            echo "Intento de push #$intento..."
            if git pull --rebase origin main && git push origin main; then
              echo "Push exitoso."
              break
            fi
            if [ $intento -eq $maximo_intentos ]; then
              echo "Se agotaron los intentos de push."
              exit 1
            fi
            espera=$(( (RANDOM % 5) + 3 ))
            echo "Push rechazado (probablemente otro workflow empujo al mismo tiempo). Esperando ${espera}s..."
            sleep $espera
            intento=$((intento + 1))
          done
